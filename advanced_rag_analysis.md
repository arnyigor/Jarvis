# 🧠 Local Hybrid RAG System v5.2

**High-Performance Retrieval-Augmented Generation System for Local LLMs**

## 📖 Обзор

Данный проект представляет собой промышленное решение для организации **поиска по документам (RAG)** в полностью локальной среде (Local LLM / Offline). Система спроектирована для работы с большими объемами текстовых данных (книги, техническая документация, юридические базы) на русском и английском языках, обеспечивая высокую точность и скорость отклика.

Ключевая особенность — **гибридная архитектура**, объединяющая лексический (BM25) и семантический (Vector) поиск с последующим умным переранжированием (Reranking).

***

## 🚀 Ключевые возможности

*   **🔍 Гибридный поиск (Hybrid Search):** Комбинация точности ключевых слов (BM25) и понимания смысла (Embeddings).
*   **⚡ GPU-ускорение:** Полная поддержка NVIDIA CUDA для эмбеддинга и реранкинга (тестировалось на RTX 5070 Ti).
*   **🇷🇺 Глубокая поддержка русского языка:** Качественная лемматизация (`pymorphy3`) вместо простого стемминга.
*   **🛡️ Транзакционная надежность:** Механизм `commit/rollback` защищает индекс от повреждения при сбоях.
*   **🚀 Параллельная индексация:** Использование `multiprocessing` для ускорения обработки текста в 3-4 раза.
*   **🎯 Высокая точность (Recall):** Механизм "широкой воронки" (Wide Funnel) с RRF-слиянием и Cross-Encoder реранкингом.

***

## 🏗️ Архитектура

Система реализует трехступенчатый конвейер поиска (Three-Tier Retrieval Pipeline):

```mermaid
graph TD
    UserQuery[User Query] --> ParallelSearch
    
    subgraph Tier 1: Parallel Retrieval
        ParallelSearch --> BM25[Lexical Search (BM25-ANN)]
        ParallelSearch --> Vector[Semantic Search (ChromaDB)]
    end
    
    BM25 -->|Top-50 Candidates| Fusion
    Vector -->|Top-50 Candidates| Fusion
    
    subgraph Tier 2: Fusion
        Fusion[Reciprocal Rank Fusion (RRF)] -->|Top-60 Unified List| RerankInput
    end
    
    subgraph Tier 3: Reranking
        RerankInput --> CrossEncoder[Cross-Encoder (Reranker)]
        CrossEncoder -->|Top-5 Final Docs| LLMContext
    end
    
    LLMContext --> LLM[Local LLM (Generation)]
```

### Компоненты

1.  **Векторное хранилище (ChromaDB):**
    *   Использует модель `intfloat/multilingual-e5-small`.
    *   Хранит семантические векторы чанков.
    *   Обеспечивает поиск по смыслу ("косвенные вопросы").

2.  **Лексический индекс (BM25-ANN):**
    *   **Уникальная реализация:** Вместо медленного инвертированного индекса используются TF-IDF векторы, загнанные в **FAISS** (IndexFlatIP).
    *   Это дает скорость поиска `O(log n)` даже для ключевых слов.
    *   Препроцессинг: Параллельная лемматизация через `pymorphy3` (вынесена в отдельные процессы для обхода GIL и Memory Leaks).

3.  **Reranker (Cross-Encoder):**
    *   Модель: `cross-encoder/ms-marco-MiniLM-L-12-v2` (или аналог).
    *   Получает на вход 60 кандидатов, попарно сравнивает их с запросом и выявляет истинно релевантные.
    *   Решает проблему "галлюцинаций поиска", отсеивая мусор.

***

## 📊 Бенчмарки и Производительность

Тестирование проводилось на наборе данных ~6000 чанков ("Война и мир" x 2) на оборудовании **NVIDIA RTX 5070 Ti**.

### Скорость Индексации
| Этап | Скорость | Примечание |
| :--- | :--- | :--- |
| **Embedding** | ~350 чанков/сек | GPU (Batch size 32) |
| **Лемматизация** | ~140 чанков/сек | CPU (6 workers, multiprocessing) |
| **Vector Indexing**| Мгновенно | FAISS + ChromaDB |
| **Итого** | ~50 сек на книгу | Полная переиндексация |

### Скорость Поиска (Latency)
| Этап | Время |
| :--- | :--- |
| **BM25 Search** | < 10 ms |
| **Semantic Search** | < 20 ms |
| **Reranking (60 docs)** | ~100 ms |
| **Total Pipeline** | **~150 ms** |

### Тест "Needle in a Haystack"
Система успешно проходит тест на поиск уникального факта ("иголки"), спрятанного в середине огромного текста.
*   **Результат:** 100% (Иголка всегда в Top-1).
*   **Причина успеха:** Расширенное окно RRF (60 документов) позволяет Cross-Encoder'у увидеть правильный ответ, даже если первичный поиск поставил его на 40-е место.

***

## 🛠️ Установка и Настройка

### Требования
*   Python 3.10+
*   NVIDIA GPU (рекомендуется, но работает и на CPU)
*   RAM: 16GB+ (для комфортной работы с моделями)

### Конфигурация (`HybridConfig`)

```python
config = HybridConfig(
    # Основные настройки
    static_docs_dir=Path("./docs"),
    chromadb_dir=Path("./chromadb"),
    
    # Модели
    embedding_model="intfloat/multilingual-e5-small",
    
    # Параметры поиска (Wide Funnel)
    top_k_bm25=50,       # Широкий захват ключевых слов
    top_k_semantic=50,   # Широкий захват по смыслу
    top_k_final=5,       # Сколько отдать в LLM
    
    # Производительность
    embedding_batch_size=32,
    enable_progress_bars=True
)
```

### Запуск

1.  **Индексация документов:**
    Система автоматически сканирует папку `docs/` при старте. Если файлы изменились, запускается инкрементальная индексация.
    ```python
    rag = HybridRAGSystem(config)
    rag.index_static_documents()
    ```

2.  **Поиск:**
    ```python
    results = await rag.hybrid_search("Что такое RAG?")
    ```

***

## 🔧 Особенности реализации (Deep Dive)

### 1. Защита от Memory Leaks на Windows
Python на Windows использует метод `spawn` для создания процессов. Это приводило к дублированию загрузки `torch` в память и ошибке `WinError 1455`.
**Решение:** Лемматизация вынесена в изолированный модуль `text_utils.py`, который не импортирует тяжелые библиотеки ML. Воркеры потребляют всего ~50МБ памяти.

### 2. Транзакционная Индексация
Для защиты базы данных используется паттерн `Two-Phase Commit`.
*   Сначала данные готовятся в памяти и временных файлах.
*   Затем обновляется `ChromaDB` и `FAISS`.
*   В самом конце атомарно обновляется `index_cache.json`.
*   При ошибке (Ctrl+C) происходит автоматический `rollback`.

### 3. BM25 через FAISS
Вместо классических библиотек (`rank_bm25`), которые медленные на Python, мы считаем TF-IDF матрицу (`scikit-learn`) и индексируем её в `FAISS IndexFlatIP`. Это позволяет искать по ключевым словам со скоростью векторного поиска.

***

## 🔮 План развития (Roadmap)

*   [x] Гибридный поиск (BM25 + Vector)
*   [x] GPU Reranking
*   [x] Параллельная индексация
*   [ ] Поддержка OCR для PDF/изображений
*   [ ] Веб-интерфейс (Streamlit/Gradio)
*   [ ] Интеграция с API OpenAI (как альтернатива локальным моделям)